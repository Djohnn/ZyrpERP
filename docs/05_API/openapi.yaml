openapi: 3.1.0
info:
  title: Enterprise Commerce Platform API
  version: 0.1.0
  description: Initial contract skeleton. Domain operations require capability PRDs.
servers:
  - url: http://localhost:8000/api/v1
    description: Local
  - url: https://staging.example.invalid/api/v1
    description: Staging placeholder domain, never production
paths:
  /health:
    get:
      operationId: getHealth
      summary: Return service health
      security: []
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status: { type: string, const: ok }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    Money:
      type: object
      required: [amount, currency]
      properties:
        amount: { type: string, pattern: "^-?[0-9]+(\\.[0-9]{1,4})?$" }
        currency: { type: string, pattern: "^[A-Z]{3}$" }
    TenantContext:
      type: object
      required: [tenant_id]
      properties:
        tenant_id: { type: string, format: uuid }
        company_id: { type: [string, "null"], format: uuid }
        branch_id: { type: [string, "null"], format: uuid }
    Problem:
      type: object
      required: [type, title, status, code, correlation_id]
      properties:
        type: { type: string, format: uri-reference }
        title: { type: string }
        status: { type: integer, minimum: 400, maximum: 599 }
        detail: { type: string }
        instance: { type: string, format: uri-reference }
        code: { type: string }
        correlation_id: { type: string }
security:
  - bearerAuth: []

